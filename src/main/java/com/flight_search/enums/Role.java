package com.flight_search.enums;

/**
 * Defines the roles available within the application.
 * These roles determine the level of access and permissions a user has.
 */
public enum Role {

    /**
     * Represents a standard user with basic permissions.
     */
    USER,

    /**
     * Represents an administrative user with elevated permissions.
     */
    ADMIN
}
